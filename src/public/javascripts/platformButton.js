const link = document.querySelectorAll('.link')

if (link.match('comic.naver.com')) button((type = 'button')) | 네이버

console.dir(link)
// if (toon.platform.includes('comic.naver.com'))
//   button((type = 'button')) | 네이버
console.log('ddd')
