extends base.pug

block content 
    if errorMessage
        span=errorMessage
    form(method="POST")
        label(for='title') 제목
        input(name="title", type="text", required)

        label(for='writer') 작가
        input(name="writer", type="text",  required)
        small 슬래쉬(/)로 나눠 주세요 

        label(for='plot') 줄거리
        input(name="plot", type="text", required)

        label(for='hashtags') 키워드
        input(name="hashtags", type="text", required)
        small 쉼표(,)로 나눠 주세요 

        label(for='platform') 링크
        input(name="platform", type="text", required)
        input(type="submit", value="Upload Webtoon")